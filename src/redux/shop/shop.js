import ShopData from '../../screens/shop/ShopData';


const initialState = {
    collections: ShopData
}

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
};